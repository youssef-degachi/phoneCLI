PORT=4242
HOST=0.0.0.0
NODE_ENV=development

# Auth - Generate hash using: node -e "require('bcrypt').hash('YOUR_PIN',10).then(console.log)"
PIN_HASH=$2b$10$PLACEHOLDER_REPLACE_ME

# JWT - Change this to a long random secret
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET_MIN_32_CHARS
JWT_EXPIRY=7d
